name: covid_stats
description: an apps to see stats of corona virus

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  #network
  rxdart: ^0.23.1
  http: ^0.12.0+4
  provider: ^4.0.4
  json_annotation: ^3.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.8.0
  json_serializable: ^3.2.5


flutter:

  uses-material-design: true

